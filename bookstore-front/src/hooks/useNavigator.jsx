/*
import { useNavigate } from "react-router-dom";
import { useEffect } from "react";

// 커스텀 훅으로 변경
function useNavigator(location) {
  const navigate = useNavigate();

  useEffect(() => {
    window.scrollTo({ top: 0, behavior: "smooth" });
    navigate(location);
  }, [location, navigate]);
}

export default 
*/